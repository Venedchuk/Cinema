@using Cinema.Models
@model Cinema.ViewModels.RandomMovieViewModel
@{
    ViewBag.Title = "Random";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var popular = Model.Customers.Count >= 2 ? "popular" : null;
}

   
@*<h2>@( ((Movie) ViewData["Movie"]).Name)</h2>   
<h2>@ViewBag.RandomMovie.Name</h2>*@
<h2 class="@popular">@Model.Movie.Name</h2>

@if (Model.Customers.Count == 0)
{
    <text>anyone has not seen this film</text>
}
else
{
        foreach (var item in Model.Customers)

        {
             <div>Cust:  @item.Name</div>

        }

}

